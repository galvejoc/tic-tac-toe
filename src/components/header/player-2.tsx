import { playerInterface } from "@/interface";

export default function Player2({ name, icon, autoplayer }: playerInterface) {
  return (
    <div>
      {icon} {name}
      <br />
      dd
    </div>
  )
}
